<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AsistenteJudicialApp.Views.Detail"
             Title="Mis Procesos">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="10" Spacing="10" BackgroundColor="LightGray">
                <Frame>
                    <StackLayout Orientation="Vertical">
                        <Label Text="Añade tus procesos para revisión" FontAttributes="Bold" FontSize="Medium" TextColor="Black" HorizontalOptions="Center"/>
                        <Button x:Name="agregarProceso" BackgroundColor="#061d34" BorderRadius="20" TextColor="White" Text="Agregar Proceso" Clicked="AgregarProceso_Clicked"/>
                    </StackLayout>
                </Frame>
                <Frame>
                    <StackLayout Orientation="Vertical">
                        <Label Text="Lista de Procesos" FontAttributes="Bold" FontSize="Medium" TextColor="Black" HorizontalOptions="Center"/>
                        <ListView   x:Name="lstProcesos" HasUnevenRows="True" ItemTapped="LstProcesos_ItemTapped">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell>
                                        <StackLayout Padding="10,0">
                                            <Label Grid.Column="1" Text="{Binding radicacion}" FontAttributes="Bold" FontSize="Medium" TextColor="Black" />
                                            <Label Grid.Column="2" Text="{Binding juzgado}"  FontSize="Medium" TextColor="Black" />
                                        </StackLayout>
                                    </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                </Frame>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>